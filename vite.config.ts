import {defineConfig} from 'vite-plus'

export default defineConfig({
	fmt: {
		bracketSpacing: false,
		printWidth: 120,
		semi: false,
		singleQuote: true,
		useTabs: true,
		ignorePatterns: [],
	},
})
